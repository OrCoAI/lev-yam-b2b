# .env.example — copy this to .env and fill in real values
# NEVER commit .env to git

# Cloudflare
CLOUDFLARE_ACCOUNT_ID=

# Grow Payments
# The webhookKey Grow sends with every webhook POST — confirmed by Grow support
GROW_WEBHOOK_KEY=
GROW_ENV=production

# Supabase
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_TABLE=levyam-b2b

# Cloudflare Worker
WORKER_URL=https://lev-yam-webhook.orcohenwork.workers.dev

# GoDaddy (for DNS reference only — manage via dashboard)
# levyam.com DNS managed at: https://dcc.godaddy.com

